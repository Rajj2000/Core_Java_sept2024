package com.cdac.anonymous;

/**
 * Problem 2: Anonymous Inner Class Extending an Abstract Class Task: Create an
 * abstract class Shape with an abstract method draw(). Write a method
 * createShape() in another class that uses an anonymous inner class to extend
 * Shape and provides an implementation for the draw() method.
 */

abstract class Shape {
	abstract void draw();
}
